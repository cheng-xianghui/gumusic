import {reqDetailInfo} from '@/api'
const state={
        detailInfo:{}
}
const mutations={
     recive_detailinfo=({comitt})
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}