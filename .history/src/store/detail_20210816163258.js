import {reqDetailInfo} from '@/api'
const state={
        detailIn
}
const mutations={

}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}