import {reqGetSearchList} from '@/api'

const state={
  searchList:[]
}
const mutations={
   recive_searchlist(state,)
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}