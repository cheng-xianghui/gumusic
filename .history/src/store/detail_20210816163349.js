import {reqDetailInfo} from '@/api'
const state={
        detailInfo:{}
}
const mutations={
     recive_deta
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}