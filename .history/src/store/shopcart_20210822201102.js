import {reqAddUpdateShopCart} from '@/api'
const state={

}
const mutations={

}
const actions={
    getAddshop({commit},skuId,skuNum){
        
    }
}
const getters={

}
export default{
    state,
    mutations,
    actions,
    getters
}