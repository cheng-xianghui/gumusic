import {reqAddUpdateShopCart} from '@/api'
const state={

}
const mutations={

}
const actions={
    getAddshop({commit},sku)
}
const getters={

}
export default{
    state,
    mutations,
    actions,
    getters
}