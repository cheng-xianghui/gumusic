import {reqGetSearchList} from '@/api'

const state={
  searchList:[]
}
const mutations={
   recive_
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}