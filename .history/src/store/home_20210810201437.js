import {reqCategoryList} from '@/api'
const state={
    // 保存数据
}
const mutations={
    // 直接修改数据
}
const actions={
    // 与组件当中用户对接
}
export{
    state,
    mutations,
    actions,
    getters
}