import {reqGetSearchList} from '@/api'

const state={
  searchList:[]
}
const mutations={
   
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}