import {reqDetailInfo} from '@/api'
const state={
        detailInfo:{}
}
const mutations={
     RECIVE_DETAILINFO=({commit}){
        this.
     }
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}