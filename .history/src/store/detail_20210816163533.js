import {reqDetailInfo} from '@/api'
const state={
        detailInfo:{}
}
const mutations={
     RECIVE_DETAILINFO=(state,detailInfo){
        this.detailInfo=detailInfo
     }
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}