import {reqCategoryList} from '@/api'
const state={
    // 保存数据
}
const mutations={
    // 直接修改数据
}
const actions={
    // 与组件当中用户对接，一般是异步发送请求，提交mutations
}
export{
    state,
    mutations,
    actions,
    getters
}