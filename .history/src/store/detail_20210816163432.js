import {reqDetailInfo} from '@/api'
const state={
        detailInfo:{}
}
const mutations={
     recive_detailinfo=({commit}){
         
     }
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}