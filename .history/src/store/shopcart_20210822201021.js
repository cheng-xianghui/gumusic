import {reqAddUpdateShopCart} from '@/api'
const state={

}
const mutations={

}
const actions={
    getAddshop({cmi})
}
const getters={

}
export default{
    state,
    mutations,
    actions,
    getters
}