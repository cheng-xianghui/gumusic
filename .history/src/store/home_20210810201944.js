import {reqCategoryList} from '@/api'
const state={
    // 保存数据
    categoryList:[]
}
const mutations={
    // 直接修改数据
    receive_categoryist
}
const actions={
    // 与组件当中用户对接，一般是异步发送请求，提交mutations
}
const getters={
    // 化简代码
}
export{
    state,
    mutations,
    actions,
    getters
}