import {reqGetSearchList} from '@/api'

const state={
  searchList:[]
}
const mutations={
   re
}
const actions={

}
const getters={

}

export default{
    state,
    mutations,
    actions,
    getters
}